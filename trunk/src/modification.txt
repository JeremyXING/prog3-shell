Revision 30:
Patch pour éviter la création d'un processus même si la commande executée est simple et builtin

Implémentation de popd et pushd (l'écriture pourra peut-être être soignée par la suite...)
